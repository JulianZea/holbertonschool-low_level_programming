#include "main.h"

/**
 * _isdigit - code
 * Description : Evaluate if it is higher or lower
 *@c:The character
 * Return: Returns: 1 if greater, otherwise 0
 */

int _isdigit(int c)
{
	if (c >= 48 &&  c <= 57)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
