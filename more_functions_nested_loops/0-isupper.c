#include "main.h"

/**
 * _isupper - code
 * Description : function that checks the uppercase character
 *@c:The charaacter
 * Return: Returns: 1 if greater, otherwise 0
 */

int _isupper(int c)
{
	if (c >= 'A' &&  c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
